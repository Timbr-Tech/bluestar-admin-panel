/* eslint-disable */
import cn from "classnames";
import styles from "./index.module.scss";

const Home = () => {
  return <div className={cn("container", styles.container)}>Home</div>;
};

export default Home;
