/* eslint-disable */
import cn from "classnames";
import styles from "./index.module.scss";

const Availability = () => {
  return <div className={cn("container", styles.container)}>Availability</div>;
};

export default Availability;
