/* eslint-disable */
import cn from "classnames";
import styles from "./index.module.scss";

const Dashboard = () => {
  return <div className={cn("container", styles.container)}>Dashboard</div>;
};

export default Dashboard;
